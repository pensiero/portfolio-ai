# Dependencies
node_modules/

# Visitor question logs (may contain personal data)
logs/
*.log

# Private biography — the real bio NEVER goes in a public repo.
# Only the sanitized template (content/private-context.example.md) is tracked.
content/private-context.md
content/*.private.md

# Secrets / environment (for the future cloud provider swap)
.env
.env.*
!.env.example

# OS / editor
.DS_Store
.idea/
.vscode/
